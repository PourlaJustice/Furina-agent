# 芙宁娜桌面 AI 桌宠 · 功能清单

> 基于 Electron + TypeScript + Vite + PixiJS + Live2D 的桌面 AI 桌宠。
> 文档更新日期：2026-08-05

## 一、项目简介

芙宁娜（《原神》角色）桌面 AI 桌宠，具备完整的「看得见、听得懂、说得出、记得住、会动手」能力链：

- **看得见**：Rust 原生截屏 + 千问视觉大模型识图/OCR
- **听得懂**：语音输入（按住说话）+ DeepSeek 对话
- **说得出**：MiniMax 语音合成朗读，与动作表情同步
- **记得住**：三层记忆系统 + 知识库 RAG 检索
- **会动手**：18 个内置工具 + 6 个 MCP 外部服务（66 个工具）

---

## 二、功能总览

| 模块 | 功能 |
| ---- | ---- |
| 🖼️ 桌宠本体 | 透明无边框置顶窗口、待机动画、眨眼/呼吸、鼠标跟随、单击动作/双击表情/拖动、高 DPI |
| 💬 AI 聊天 | DeepSeek 流式对话、Markdown 渲染、全屏聊天（模型+聊天同屏）、历史保留/清空 |
| 🎙️ 语音朗读 | MiniMax TTS 逐句合成、喇叭重听、口型同步 |
| 🎤 语音输入 | 按住说话实时转文字（阿里云百炼 qwen-audio），支持热词 |
| 🎭 动作表情 | 回复中的（动作/表情）自动匹配 Live2D，随语音进度触发，重听从头播放 |
| 🧠 记忆系统 | 自动提取名字/喜好/近况/目标，长期记忆，可查看/清空 |
| 📚 知识库 RAG | 导入 txt/md/json/csv/pdf/docx/xlsx/pptx，本地离线语义检索 + 世界书设定 |
| 🛠️ Agent 工具 | 18 个内置工具，危险操作弹窗确认 + 信任记忆 |
| 🔗 MCP 服务 | filesystem / playwright / netease-music / sqlite / qweather / qwen-vision，共 66 个工具 |
| 🎵 网易云音乐 | 点歌、搜索、歌词、播放控制、迷你点歌台（Web 播放器） |
| 🌤️ 天气 | 内置 2 日天气 + 和风 15 天预报/空气质量/生活指数/预警 |
| 🗄️ SQLite 数据库 | 聊天记录、会话、统计持久化 |
| 👀 视觉理解 | 截图后调用千问看图/OCR，识别屏幕内容 |
| 📦 打包分发 | 全量自包含包（便携 Node + 浏览器 + 播放器），一键启动 |

---

## 三、详细功能

### 1. 桌宠本体（Live2D 渲染）

- 透明无边框、置顶、可拖动的桌面小窗（305×505）
- 待机动画循环播放，程序化眨眼、呼吸
- 鼠标跟随：眼珠与头部随光标转动
- 单击触发动作、双击触发表情
- 高 DPI 适配，自定义窗口缩放
- 支持全屏模式：模型与聊天分栏（模型位置可左右切换）

### 2. AI 聊天

- **模型**：DeepSeek（SSE 流式输出）
- **人设**：完整芙宁娜角色提示词（性格、语言风格、情绪演绎、当前状态与对话规则）
- **Markdown 渲染**：聊天内容支持代码块、列表等实时渲染
- **全屏聊天窗**：模型（腰部以上）与聊天同屏，可互相切换布局
- 对话历史按窗口独立保留，可一键清空
- 点击消息中的链接自动用系统浏览器打开

### 3. 语音朗读（TTS）

- MiniMax 语音合成，逐句合成并播放
- 每条回复带「喇叭」按钮，点击朗读 / 重听
- 朗读时口型同步（嘴部动作跟随语音）
- 支持自定义音色 ID 与音色克隆脚本（`voice/minimax-furina-voice.mjs`）
- 语音失败不阻塞聊天，静默跳过

### 4. 语音输入（ASR）

- 按住麦克风按钮说话，实时转文字填入输入框
- 阿里云百炼实时语音识别（`qwen-audio-3.0-asr-flash-streaming`）
- 支持热词配置（提升人名、专有名词识别准确率）
- 配置自动回退环境变量 `DASHSCOPE_API_KEY`

### 5. 动作表情联动

- 回复中的（动作/表情描述）自动匹配 Live2D 动作与表情
- 动作库：待机、变芒、变荒、摊手等；表情库：眨眼、托脸、猫猫嘴、小脸红、生气、委屈、哭等 20+ 种
- 动作与语音进度同步：朗读到对应位置才触发，不瞬间堆叠
- 点击重听时从头同步重放动作
- 全部动作做完后回到原始姿态

### 6. 记忆系统（三层）

- 自动提取并长期记住用户信息：**名字 / 喜好 / 近况 / 目标**
- 三层记忆结构，随对话持续更新
- 设置页可查看当前记忆、一键清空

### 7. 知识库（RAG）

- 支持格式：`txt / md / json / csv / pdf / docx / xlsx / pptx`
- 本地离线语义检索（bge-small-zh 嵌入模型）+ 关键词检索双通道
- 内置「世界书」设定（Worldbook）与「世界见闻」知识文件（`knowledge/`）
- 设置页可导入文件/文件夹、浏览知识库、清空重建
- 简历/个人资料导入后可被芙宁娜直接引用

### 8. Agent 内置工具（18 个）

| 分类 | 工具 | 说明 |
| ---- | ---- | ---- |
| 基础 | `get_time` | 当前日期时间 |
| 基础 | `calc` | 安全数学计算 |
| 基础 | `web_search` | Bing 网页搜索 |
| 文件 | `list_dir` / `read_file` | 浏览/读取（支持多格式文档） |
| 文件 | `write_file` / `delete_file` / `move_file` / `open_path` | 写入/删除/移动/打开 |
| 待办 | `add_todo` / `list_todos` / `complete_todo` / `delete_todo` | 待办增删查改 |
| 提醒 | `add_reminder` / `list_reminders` / `cancel_reminder` | 定时提醒，到点弹系统通知 |
| 天气 | `get_weather` | 今天/明天天气 |
| 截图 | `screen_shot` | Rust 原生截图，保存 PNG |

另有 LangGraph 专属 `knowledge_search`（知识库检索）。

### 9. MCP 外部服务（6 个服务器 / 66 个工具）

| 服务器 | 能力 |
| ---- | ---- |
| **filesystem** | 文件系统读写（危险操作确认） |
| **playwright** | 浏览器自动化：打开网页、点击、填表、抓数据（内置 Chromium 离线可用） |
| **netease-music** | 网易云点歌、搜索、歌词、播放控制、迷你点歌台 |
| **sqlite** | 直接查/写数据库（`data/furina.db`：聊天记录、会话、统计） |
| **qweather** | 和风天气：实时天气、3-30 天预报、空气质量、16 种生活指数、预警 |
| **qwen-vision** | 千问视觉：看图理解、OCR 识别（配合截图工具） |

工具命名规则：`服务器名__工具名`（如 `qweather__get-weather-now`）。

### 10. 安全机制

- **危险操作确认弹窗**：文件删除/移动、写入、截图、浏览器操作等执行前确认
- **信任记忆**：选择「本次及以后默认通过」后同类操作直接放行（`trusted-tools.json`）
- **contextBridge 隔离**：渲染进程只能访问预加载暴露的安全 API
- **密钥本地存储**：全部配置存于 `%APPDATA%/furina-agent/`，不上传；MCP 密钥文件不提交 git

### 11. 网易云音乐

- 点歌、搜歌（歌名+歌手）、切歌、暂停/继续、音量控制
- 歌词获取与转述
- 迷你点歌台（Web 播放器 `http://127.0.0.1:8765/`）：桌面小窗控制、播放进度

### 12. 天气查询

- 内置 `get_weather`：今天/明天（Open-Meteo）
- 和风天气 MCP：实时天气、15 天预报、空气质量（实时/逐小时/3 天）、16 种生活指数、天气预警
- 城市中文名直查（如「北京」）

### 13. 视觉理解

- Rust 原生截图助手（DXGI/GDI 双通道，NDJSON IPC）
- 截屏后调用 `qwen3.7-plus` 视觉模型分析内容、识别文字
- 场景：看屏幕、看窗口、OCR、图片描述

### 14. 数据库（SQLite）

- 文件：`data/furina.db`
- 表：`chat_messages`（聊天记录）、`chat_sessions`（会话）、`agent_stats`（统计）
- 芙宁娜可被要求「把对话存进数据库」「统计聊天次数」等

### 15. 设置界面

- 标签页：对话 / 语音 / 语音输入 / 记忆 / 知识库 / 信任 / 音乐 等
- 对话：DeepSeek API Key、模型、温度等
- 语音：MiniMax API Key、音色 ID、启用开关
- 记忆：查看 / 清空
- 知识库：导入、浏览、清空
- 信任：查看 / 清空已信任的危险操作
- 音乐：迷你点歌台入口

### 16. 打包与分发

- `打包.bat`：生成 1GB 全量自包含 zip（源码 + 依赖 + 模型 + 便携 Node + 浏览器 + 播放器 + 密钥）
- `一键启动.bat`：收件人解压后双击即用，无需安装任何软件
- `使用说明.txt`：随包说明
- `.gitignore` 保护：密钥/大目录不推 GitHub

---

## 四、启动与常用脚本

| 文件 | 作用 |
| ---- | ---- |
| `一键启动.bat` | 免安装启动（优先内置便携 Node） |
| `启动桌宠.bat` / `start-furina.ps1` | 后台启动桌宠 |
| `关闭桌宠.bat` / `stop-furina.ps1` | 关闭桌宠 |
| `打包.bat` | 生成全量分发包 |
| `npm run dev` | 开发模式（构建 + 启动） |
| `npm run build` | 构建 main/preload/renderer |
| `voice/minimax-furina-voice.mjs` | MiniMax 音色克隆 |
| `scripts/build-screenshot-helper.mjs` | 编译 Rust 截图助手 |

---

## 五、技术栈

- **框架**：Electron 43 + TypeScript 5 + Vite 5 + Node 24
- **渲染**：PixiJS 7 + pixi-live2d-display + Cubism Core
- **LLM**：DeepSeek（SSE 流式）+ LangChain 1.x + LangGraph 1.x
- **语音**：MiniMax Speech（TTS + 克隆）、阿里云百炼 Paraformer（ASR）
- **视觉**：qwen3.7-plus / qwen-audio（百炼 DashScope）
- **MCP**：@modelcontextprotocol/sdk（stdio 传输）
- **文档解析**：mammoth（docx）、unpdf（pdf）、xlsx、jszip、markdown-it
- **原生模块**：Rust（DXGI / GDI / WIC 截图）
- **数据库**：better-sqlite3（Node 24 预编译版）

---

## 六、数据与配置存储位置

| 内容 | 位置 |
| ---- | ---- |
| 聊天/模型配置 | `%APPDATA%/furina-agent/chat-config.json` |
| 记忆 | `%APPDATA%/furina-agent/memory.json` |
| 待办/提醒 | `%APPDATA%/furina-agent/tasks.json` |
| 信任操作记录 | `%APPDATA%/furina-agent/trusted-tools.json` |
| 语音识别配置 | `%APPDATA%/furina-agent/asr-config.json` |
| 嵌入模型缓存 | `%APPDATA%/furina-agent/models/` |
| 聊天记录/统计 | 项目 `data/furina.db` |
| 知识库导入 | 项目 `knowledge/`（含 `genshin-updates.md`） |
| MCP 服务器配置 | 项目 `mcp-servers.json`（含密钥，不提交 git） |

---

## 七、已完成学习阶段

| 阶段 | 内容 |
| ---- | ---- |
| 0-1 | Electron + TypeScript 骨架、三进程模型、IPC、Vite |
| 2 | Live2D 渲染、透明置顶窗口 |
| 3 | DeepSeek 流式聊天 |
| 4 | MiniMax TTS 语音朗读 |
| 5 | 动作/表情联动（含语音进度同步） |
| 6 | 三层记忆 + 多格式知识库 RAG |
| 7 | Agent 工具系统 + LangChain/LangGraph |
| 8 | MCP 外部服务集成 |
| 9 | Rust 原生截图模块 |
| 附加 | 语音输入、网易云音乐、SQLite、和风天气、视觉理解、全量打包分发 |

---

## 八、注意事项

- 芙宁娜 Live2D 模型版权归 miHoYo（原神），禁止商用、禁止二次配布
- 全量包内含个人密钥与账号 Cookie，仅限信任者使用
- 视觉/语音调用消耗百炼、DeepSeek 等 API 额度
